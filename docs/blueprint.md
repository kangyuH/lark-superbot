# lark-superbot 蓝图

本文描述本项目的**最终愿景**和**分阶段路线**。当前代码仍以「分发 + 记账」为主；实现状态以 [README](../README.md) 为准，本文是方向，不是已交付清单。

## 要解决什么

业务方在飞书里 `@` 机器人提出事项（查口径、补数据、跑 pipeline、问风险）。今天真正干活仍要人打开 IDE，一条条接任务、查上下文、执行、再回飞书。

愿景是反过来：

- **基础操作自动完成**，不必经维护者手
- **需要人工决策的工作自动分流**到台账，等人给方向
- **用户端只在飞书**询问现状、评估风险、给出方向性决策
- 决策之后 AI 按已有剧本继续，直到完成或再次卡住
- 维护者打开 IDE 变成例外，而不是日常工位

成功的主指标：**大多数事项从入站到闭环，不再依赖 Cursor / IDE 会话。** 建议按单量看，目标约 **70%**。这 70% 指「次数」——询问类、已知剧本类；不是「所有难度的活都能无人值守」。

## 最终形态

```text
用户（业务群或台账）
    │  询问现状 / 评估风险 / 给方向 / 催办
    ▼
分发环          只判断：新事项 / 跟旧任务 / 无关
    ▼
探查环          只读：上下文、口径、风险、选项 → 写入任务
    ▼
门禁
    ├─ 低风险只读          → 直接回答，短回执回业务群
    ├─ 有剧本且可执行      → 执行环自动往下跑
    └─ 无剧本 / 不可逆     → waiting_human，台账 @关注方
         ▲                        │
         │                        │ 方向性决策（飞书卡片或跟进）
         └──── 执行环 ←───────────┘
                    │
                    ▼
              完成 / 再次卡住 / 需外部资源
```

两条飞书表面，不要合成一个群：

| 表面 | 给谁看 | 写什么 |
|------|--------|--------|
| 业务群 | 提出问题的人 | **短回执**：已记下、已跟进哪条任务、结论一句话。不刷长推理 |
| 台账群 thread | 维护者与关注方 | **过程与决策**：初探、风险、选项、status 变更、执行日志 |

IDE（Cursor 等）的定位变成：**编写和验收新剧本**（工具、skill、允许清单），以及处理门禁拦住的疑难。不是每条任务的运行时。

## 70% 怎么理解

| 算进 70% | 不算、应分流给人 |
|----------|------------------|
| 「现在怎样了」「风险大不大」 | 第一次定口径、对错没有客观脚本 |
| 按已验收剧本入库 / 查 SQL / 巡检 | 无匹配剧本的写操作 |
| 决策已写明，执行器可接着跑 | 不可逆、超预算、校验失败 |
| 日巡发现无进展并提醒关注方 | 数准争议、历史冻结策略、未知故障 |

反指标：任务仍要挂上 Cursor 会话线索才能推进的，就是还没自动化的那部分。

## 非目标

- 用一个全能 agent 代替维护者在 IDE 里写代码、探未知故障
- 让 **Dispatcher（分发）兼执行**——误建/noop 一旦变成改数，代价不可接受
- 在业务群输出长篇推理或命令过程
- 把本服务做成第二个飞书 OpenAPI 网关；飞书原子能力仍走 `lark-cli` / 既有飞书技能
- 用共享静态 token 当长期多人身份方案（可作内网起步，不是终态）

## 目标架构

### 三个环

职责必须分开，可以同机不同进程，但工具白名单不能混。

| 环 | 职责 | 禁止 |
|----|------|------|
| **分发** | 入站消息 → create / followup / noop，留痕 | 回长文、改业务数据、把任务直接推到执行中 |
| **探查** | 拉上下文、开放任务、项目知识、线索；写初探 note；评估风险与选项 | 写业务侧；在没有门禁通过时跑剧本 |
| **执行** | 只调用**允许清单**上的剧本；写 followup；成功则收尾或再等人确认 | 泛化编码、越权写、失败假装成功 |

Gateway 继续是账本：任务、事件、线索、队列、台账同步。Agent 不直接改 `data/`。

### 门禁

每条任务（或每个执行步骤）先分类：

1. **低风险只读**：查任务、查数、解释已有口径 → 探查后即可答
2. **可逆、有剧本的写**：已验收的入库、重跑、固定巡检 → 可自动，或只确认一句
3. **不可逆 / 无剧本 / 超预算** → 必须 `waiting_human`，方向写进台账后再进入执行环

`kind`（`readonly` / `operational`）继续表示任务性质；门禁是额外的 **风险 × 是否有剧本**，不要把「operational」理解成「可以随便自动跑」。

状态机沿用现有约定，语义要撑得住闭环：

| status | 在愿景里的含义 |
|--------|----------------|
| `noted` | 刚记下，探查尚未开始（应尽快被探查环接走） |
| `agent_running` | 探查或执行正在进行 |
| `waiting_human` | 要方向、要确认、或一轮已结束等人看 |
| `waiting_external` | 等协助方给材料 |
| `blocked` | 条件不成立，巡检应反复打出 |
| `done` / `cancelled` | 终态；仍可追加 note，不可再改 status |

### Agent 工具面

对外给「会调工具的模型」的，应是 **可发现的工具/资源契约**（MCP：`tools/list`、`resources/read`），而不是 skill 里的 curl 手册。

- REST 保留给本仓库 worker、运维、注册 bot、队列 claim
- MCP（或同等协议）只暴露 Agent 面：任务读写跟进、线索、只读上下文、允许清单内的执行入口
- 不要从 OpenAPI 全量生成工具，避免把 `bots/register`、队列 ack、随意 `im/send` 交给模型
- 读多写少的材料用 resource（任务摘要、timeline、线索、项目知识），避免一次把详情塞爆上下文

分发环可以继续用内部工具包；中长期应与探查/执行共用同一套领域动作，避免「Dispatcher 能做、飞书执行器还要再教一遍」。

### 关注方与决策入口

- 任务带 **关注方**列表；台账同步消息 @ 全部关注人
- 被加入的人可在飞书取消关注，历史消息不改写
- 台账 thread 提供交互跟进（卡片按钮优先于口令）：改 status、追加 note、写下方向
- 业务群短回执与台账写回是两条路，不要把业务群 @ 当成改状态的主入口

## 现在到终态差什么

| 已有 | 还没有 |
|------|--------|
| 业务群 @ → 入队 → Dispatcher 记账 | 消费后的飞书短回执 |
| 任务 + 事件 + 线索 + HTTP API | 创建后的自动初探 |
| 台账根帖 + thread 单向同步 | 飞书端把跟进写回任务 |
| 单一登录用户被 @ | 关注方、取消关注 |
| Cursor skill + curl 调 REST | 可发现的 MCP 工具面 |
| 人在 IDE 里当执行器 | headless 执行 worker + 允许清单 + 门禁 |
| 无定期回顾 | 日度巡检未关闭任务与昨日进展 |

## 路线

原则：**先让飞书能看见、能决策，再让只读探查自动跑，最后才允许清单内的自动执行。** 不要等协议层或全能执行器齐活再做体验。

### 第 0 阶段 — 飞书控制面

目标：查询和决策不必开 IDE。

- inbound 消费后自动短回执（已建任务 / 已跟进 / 为何 noop；失败不要假装成功）
- 台账 thread 交互跟进（卡片触发 followup，遵守终态规则）
- 任务关注方；同步消息 @ 全员；飞书取消关注

验收：一周内「只想知道任务怎样了 / 记一句决策」可以全程在飞书完成。

### 第 1 阶段 — 只读闭环

目标：询问现状和评估风险有自动初稿，停住的任务会被打出来。

- 任务创建后自动一轮探查（上下文、知识、缺口、选项），写入 followup，再进入 `waiting_human`
- 探查失败也要留下 note，任务不得默默停在 `noted`
- 日度巡检：所有未关闭任务 + 前一日进展；无进展、阻塞过久、台账同步失败要标出
- 业务群仍然只回短句；长文在台账

验收：用户问「评估风险」「卡在哪」，台账里已有一版答案；维护者主要处理巡检标红项。

本阶段**可以还没有执行环**。方向性决策先人工或仍由 IDE 落地，但分流已经发生。

### 第 2 阶段 — 决策后能接着跑

目标：单量意义上逼近 70%。缺的是 **headless 执行 worker**，不是更多 REST。

- 同进程或同实例提供 MCP（Streamable HTTP）：Agent 可发现工具；REST 不动
- 执行器只认允许清单（先从每周重复的 3～5 个剧本起：固定 SQL、已验收入库、健康/tag 巡检等）
- 台账记下方向 → `agent_running` → 跑剧本 → followup → `waiting_human` 或 `done`
- 无匹配剧本、越权、校验失败 → 停并写明原因，不升级成通用编码 agent
- 门禁落地：只读自动答；有剧本的写可自动或一键确认；其余等人

验收：至少一类重复事项从 `@` 到回执不需要 Cursor 会话。稳定剧本数到 5～8 条后，再按「无 IDE 线索即闭环」的占比看 70%。

MCP 在本阶段才划算：探查、执行、外接 IDE 调试共用工具面。第 0/1 阶段不要被它挡住。

### 第 3 阶段 — 维护者成为例外处理器

- 同一类手搓出现 2～3 次 → 收成剧本并挂进允许清单
- 日巡 + 关注方：真正要拍板的才 @ 到人
- 业务方自己问现状、给方向；维护者只接无剧本、高风险、巡检失败
- 身份从「全站共享 token + 客户端自报 actor」演进到能分清谁批准、谁取消关注（内网可先静态凭证）

验收：维护者被 @ 次数下降；新剧本数相对手搓次数上升；Cursor 几乎只用于写剧本、改 Gateway、处理门禁拦截。

## 阶段指标

| 阶段 | 看什么 |
|------|--------|
| 0 | inbound 有短回执的比例；纯查询/记决策是否还要开 IDE |
| 1 | 新建任务有初探 note 的比例；未关闭任务连续无进展被巡检打出的比例 |
| 2 | **无 Cursor 会话线索即闭环的任务占比**（主指标）；执行失败且正确停住的比例 |
| 3 | 维护者被 @ 次数；新剧本 / 手搓次数 |

## 工作项对照

下列是从愿景拆出的产品工作项，实施时可在任务台账里各建一条，彼此独立，但依赖关系如下。

```text
短回执 ─┐
台账交互 ─┼─► 第 0 阶段控制面
关注方 ───┘         │
                    ▼
自动初探 ─┬─► 第 1 阶段只读闭环
日度巡检 ─┘         │
                    ▼
MCP 工具面 ──► 执行 worker + 允许清单 + 门禁 ──► 第 2 阶段
                    │
                    ▼
剧本沉淀 / 身份与权限细化 ──► 第 3 阶段
```

| 工作项 | 所属阶段 | 要点 |
|--------|----------|------|
| inbound 消费后短回执 | 0 | 幂等；与「业务群不长篇」一致 |
| 台账 thread 交互跟进 | 0 | 卡片优先；默认 app 如何收事件需单独立项 |
| 任务关注方 | 0 | 同步 @ 全员；飞书可取消 |
| 创建后自动初探 | 1 | 与 Dispatcher 分离；结束后 `waiting_human` |
| 日度巡检 | 1 | 定时，非 inbound 驱动 |
| MCP 化 | 2 | REST 旁挂 `/mcp`；只暴露 Agent 工具 |
| headless 执行器 | 2 | 允许清单；无剧本即停 |
| 门禁策略 | 2 | 风险 × 剧本；不要 Dispatcher 兼执行 |
| 剧本工厂 | 3 | IDE 只产工具，运行时在 worker |

## 原则

1. **分发、探查、执行三分开。** 分发永远不改业务。
2. **先分流，后自动。** 没有 `waiting_human` 和台账决策面，执行器只是更危险的脚本。
3. **剧本白名单，而不是通用自主。** 新能力用「出现两次再收成工具」换稳定性。
4. **业务群短、台账长。** 用户要的是结论；维护者要的是可审计过程。
5. **Agent 只走服务接口。** 禁止为了方便改账本落盘；工作区是任务附件，不是 API。
6. **用线索衡量自动化。** 还要开 IDE 才能推进的任务，就是路线图上的欠账。

## 和当前 README 的关系

README 描述**现在能跑什么、怎么接入**。本文描述**打算长成什么**。阶段 0 落地后，README 应改掉「默认不回业务群、真要动手靠人」的表述，改成与回执/台账交互一致的操作说明；在那之前以 README 的行为描述为准。
